const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function testSignup() { const { data, error } = await supabase.auth.signUp({ email: 'dummy.mindfulness.123@gmail.com', password: 'password123', options: { data: { role: 'siswa', full_name: 'Test Siswa' } } }); console.log('Signup:', error ? error.message : data.user?.email); } testSignup();
